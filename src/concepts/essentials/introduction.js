/* createRoot of ReactDOM (From react-dom/client) creates a root element, createRoot returns an Root Object 
which contains render method that can be used to render components inside root element */

/* createElement (From react) takes three props (element name, attributes (e.g. style, onClick, etc), and 
children (Either string or ReactNode[])) and create component out of it. */
